# Project Manager

Electron + Vite + React + TypeScript 本地桌面项目管理工具。

一键管理多个前端/Python 等项目的开发环境，支持自定义启动命令、独立进程管理、实时日志输出。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run dist
```
